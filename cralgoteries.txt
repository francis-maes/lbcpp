
* Optimisation de l'utilisation mémoire
* Mechante optimisation (précalcul des Perceptions)

* Voir si on peut faire du Dissulfide Bond Matrix
* Voir comment récuperer le Loss pendant l'évaluation

Boinc:
 * Gestion générique des paramètres
 * Essayer d'optimiser l'utilisation mémoire
 * Rendre le save deterministe
 * Render le load de fichiers deterministe (trier les fichiers)

Cleaning:
 * ProteinInferenceFactory: reorganiser les Perceptions, classer par variable de la protéine; implémenter le truc dans BoincEvaluator
 * Multiclass classifiers: output discrete distributions
 
Inference:
 * Segmental Features
 * Recursive parameters, use in:
    - StoppingCriterionOnlineLearner
    - InitializeByCloningBatchLearner

Extra-trees:
 * Make it really work !

Explorer:
 * Verifier la stabilité
 * Mettre à jour le ProcessManager pour qu'il utilise les classes Job et ThreadPool

Types:
 * permettre de déclarer des variables ayant des Types abstraits -> c.f. pb dans ProteinInferenceStep::ProteinInferenceStep
