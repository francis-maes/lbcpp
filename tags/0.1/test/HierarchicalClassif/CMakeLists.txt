INCLUDE_DIRECTORIES(
  ${LBCpp_SOURCE_DIR}/include
  ${CMAKE_CURRENT_BINARY_DIR}
)

SET(HIERARCHICAL_CLASSIF_LHEADERS
  HierarchicalClassif.lh
)

ADD_CRALGORITHM_SOURCES(${HIERARCHICAL_CLASSIF_LHEADERS})

SET(HIERARCHICAL_CLASSIF_SOURCES
  ${CRALGORITHMS_OUTPUT_FILES}
  Main.cpp
)

SOURCE_GROUP(GeneratedCode FILES ${CRALGORITHMS_OUTPUT_FILES})

ADD_EXECUTABLE(HierarchicalClassif ${HIERARCHICAL_CLASSIF_SOURCES})

TARGET_LINK_LIBRARIES(HierarchicalClassif lbcpp-core)
