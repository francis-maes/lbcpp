INCLUDE_DIRECTORIES(
  ${LBCpp_SOURCE_DIR}/include
  ${CMAKE_CURRENT_SOURCE_DIR}
)

ADD_EXECUTABLE(unzipper${NAME_FILE_SUFFIX}
  Main.cpp
)

TARGET_LINK_LIBRARIES(unzipper${NAME_FILE_SUFFIX}
  lbcpp-core${NAME_FILE_SUFFIX}
)

