INCLUDE_DIRECTORIES(
  ${LBCpp_SOURCE_DIR}/include
  ${CMAKE_CURRENT_SOURCE_DIR}
)

# RunWorkUnit
ADD_EXECUTABLE(RunWorkUnit${NAME_FILE_SUFFIX} RunWorkUnit.cpp)
TARGET_LINK_LIBRARIES(RunWorkUnit${NAME_FILE_SUFFIX} lbcpp-core${NAME_FILE_SUFFIX})

# unzipper
ADD_EXECUTABLE(unzipper${NAME_FILE_SUFFIX} unzipper.cpp)
TARGET_LINK_LIBRARIES(unzipper${NAME_FILE_SUFFIX} lbcpp-core${NAME_FILE_SUFFIX})

# SendWorkUnit
IF(LBCPP_NETWORKING)
  ADD_EXECUTABLE(SendWorkUnit${NAME_FILE_SUFFIX} SendWorkUnit.cpp)
  TARGET_LINK_LIBRARIES(SendWorkUnit${NAME_FILE_SUFFIX} lbcpp-core${NAME_FILE_SUFFIX})
ENDIF(LBCPP_NETWORKING)

# Debug Pure Virtual
ADD_DEFINITIONS(-DDEBUG_PURE_VIRTUAL)
ADD_EXECUTABLE(DebugPureVirtual${NAME_FILE_SUFFIX} DebugPureVirtual.cpp)
TARGET_LINK_LIBRARIES(DebugPureVirtual${NAME_FILE_SUFFIX} lbcpp-core${NAME_FILE_SUFFIX})
