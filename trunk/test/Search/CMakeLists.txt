INCLUDE_DIRECTORIES(
  ${LBCpp_SOURCE_DIR}/include
  ${CMAKE_CURRENT_BINARY_DIR}
)

SET(SEARCH_LHEADERS
  LCeB.lh
  NQueens.lh
)

ADD_CRALGORITHM_SOURCES(${SEARCH_LHEADERS})

SET(SEARCH_SOURCES
  ${CRALGORITHMS_OUTPUT_FILES}
  Main.cpp
)

SOURCE_GROUP(GeneratedCode FILES ${CRALGORITHMS_OUTPUT_FILES})

ADD_EXECUTABLE(Search ${SEARCH_SOURCES})

TARGET_LINK_LIBRARIES(Search lbcpp-core)
