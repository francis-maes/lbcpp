INCLUDE_DIRECTORIES(
  ${LBCpp_SOURCE_DIR}/include
  ${CMAKE_CURRENT_BINARY_DIR}
  ${CMAKE_CURRENT_SOURCE_DIR}
)

GENERATE_INTROSPECTION_CODE(GlopClasses.xml GlopClasses.cpp)

SET(LBCPP_GLOP_SOURCES
  OFQI.cpp  
  GlopClasses.xml
  ${CMAKE_CURRENT_BINARY_DIR}/GlopClasses.cpp
)

ADD_EXECUTABLE(glop ${LBCPP_GLOP_SOURCES})
TARGET_LINK_LIBRARIES(glop lbcpp-core)


